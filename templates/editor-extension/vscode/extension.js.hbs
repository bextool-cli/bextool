function activate(context) {
  const command = {
    dispose() {},
  };

  console.log("{{projectName}} activated");
  context.subscriptions.push(command);
}

function deactivate() {}

module.exports = {
  activate,
  deactivate,
};
