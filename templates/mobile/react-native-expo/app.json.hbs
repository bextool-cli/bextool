{
  "expo": {
    "name": "{{projectName}}",
    "slug": "{{projectSlug}}",
    "version": "{{version}}",
    "orientation": "portrait",
    "ios": {
      "bundleIdentifier": "{{appIdentifier}}"
    },
    "android": {
      "package": "{{appIdentifier}}"
    }
  }
}
